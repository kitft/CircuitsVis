import React from "react";
import { AnyColor } from "colord";
/**
 * Token (shown as an inline block)
 */
export declare function TokenCustomTooltip({ token, value, min, max, negativeColor, positiveColor, tooltip }: {
    token: string;
    value: number;
    min: number;
    max: number;
    negativeColor?: AnyColor;
    positiveColor?: AnyColor;
    tooltip?: React.ReactNode;
}): JSX.Element;
//# sourceMappingURL=TokenCustomTooltip.d.ts.map